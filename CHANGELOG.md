# Changelog

Все заметные изменения в расширении «F5 — расшифровка переменных» документируются здесь.
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
проект придерживается [семантического версионирования](https://semver.org/lang/ru/).

## [2.0.2] — 2026-06-16

### Fixed
- Тестовый режим формул по сущности: цепочки модификаторов после `:calc`
  теперь применяются корректно; `floor(N)`/`ceil(N)` учитывают точность.

## [2.0.1] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены оставшиеся базовые модификаторы
  шаблонизатора, кроме `timezone`, `region`, `qr`, `insert*`, `row`.

## [2.0.0] — 2026-06-16

### Changed
- Окно тестового режима формул по сущности теперь можно растягивать по всем
  направлениям; размер и позиция сохраняются в `chrome.storage.local`.

## [1.3.23] — 2026-06-16

### Changed
- Обновлён код доступа для тестового режима формул по сущности.

## [1.3.22] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: скрыто сообщение успешной загрузки данных
  в окне проверки формулы.

## [1.3.21] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: удалены кнопка «Поля» и блок «Разбор»
  из окна проверки формулы.

## [1.3.20] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлен блок ошибок и подсветка ввода
  для синтаксиса, нераспознанных переменных и ошибок расчёта.

## [1.3.19] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлена кнопка шаблонизатора в окно,
  очищены стартовые подсказки и поправлено положение кнопки в блоке виджетов.

## [1.3.18] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: для `spell_money`/`spell_price` добавлены
  режимы `short`, `normal`, `duplication`, `clarification`.

## [1.3.17] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены базовые рабочие дни
  (пн–пт) для `addworkdays`, `nextworkday`, `previousworkday`, `isworkday`.

## [1.3.16] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены `spell_money`/`spell_price`,
  `cfm(...)` и базовая поддержка `catalogElement(first, catalogId).cf(fieldId)`.

## [1.3.15] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлена нормализация значений по типам
  amoCRM и сохранение enum-метаданных полей.

## [1.3.14] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены ФИО/транслит/списки/простая
  пропись чисел и просмотр заполненных полей загруженных сущностей.

## [1.3.13] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены явные ссылки на другие
  сущности (`lead(id)`, `contact(id)`, `company(id)`) с кешем загрузки.

## [1.3.12] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлена обработка вложенных переменных
  в аргументах модификаторов и поддержка `date.now`.

## [1.3.11] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлены строковые модификаторы и
  базовые операции с датами.

## [1.3.10] — 2026-06-16

### Changed
- Тестовый режим формул по сущности: добавлена первая группа модификаторов
  для чисел, строк, условий и дат.

## [1.3.9] — 2026-06-16

### Added
- Тестовый режим доп. функций: расчёт `:calc` по значениям текущей карточки
  amoCRM и связанных сущностей.

## [1.3.8] — 2026-05-29

### Changed
- Сапёр: выбор размера поля 16×16, 32×32, 64×64.

## [1.3.7] — 2026-05-29

### Changed
- Расширены доп. функции и dock F5.

## [1.3.6] — 2026-05-29

### Changed
- Чистка кода: удалены неиспользуемые фрагменты, упрощены стили и обработчики
  доп. функций.
- Уточнены формулировки в попапе и README (буфер обмена, сеть, доп. функции).

## [1.3.3] — 2026-05-28

### Added
- Шаблонизатор `{…}`: полный список модификаторов F5 (102 пресета) по
  [cmdf5.ru/manual/vars](https://cmdf5.ru/manual/vars) — дата/время, числа,
  прописью, склонение, условия, строки, телефон, виджет «Документы» и др.
  Селектор сгруппирован по категориям (`optgroup`). Список вынесен в
  `F5VRParser.listModifierPresets()` в `lib/parser.js` — единый источник со
  справочником расшифровщика.

## [1.3.2] — 2026-05-18

### Fixed
- Скрытие подсказок Аммы больше не белит всю страницу amoCRM: убраны
  слишком широкие селекторы `div:has(.svg-amma_chat--cross-close-dims)`,
  которые матчили корневой контейнер приложения. Оставлен только точный
  `div:has(> div > button .svg-amma_chat--cross-close-dims)`.

## [1.3.1] — 2026-05-18

### Fixed
- Скрытие Аммы теперь прячет и всплывающие подсказки («С этой сделкой что-то
  не так…») — по стабильной иконке `svg-amma_chat--cross-close-dims`, без
  хеш-классов amoCRM.
- Проверка обновлений: URL `manifest.json` исправлен на корень репозитория
  (`.../main/manifest.json`) — раньше был неверный путь с подпапкой и
  возвращался 404 («ошибка проверки»).
- Zipball-обновление: файлы из архива пишутся в корень папки расширения
  (структура репозитория F5--- без вложенной поддиректории).
- Модификатор `:df` в шаблонизаторе: пресеты и примеры используют правильное
  экранирование (`d\.m\.Y`, `H\:i`).

### Changed
- Тумблер в попапе переименован в «Амма: показана/скрыта» (кнопка + подсказки).
- При уже включённом скрытии Аммы CSS-правило обновляется при загрузке страницы,
  а не игнорируется.

## [1.3.0] — 2026-05-18

### Added
- **Автообновление расширения в один клик** через File System Access API.
  В попапе появляется баннер «Доступна версия X.Y.Z → Обновить». Клик
  открывает отдельное окно-визард, в котором расширение само скачивает
  свежий архив с GitHub (ветка `main`), распаковывает его и перезаписывает
  файлы в локальной папке расширения. Завершается `chrome.runtime.reload()`
  — ходить в `chrome://extensions` и жать ⟳ больше не нужно.
- При первом обновлении расширение однократно просит выбрать папку, из
  которой Chrome загружал распакованную версию. Хэндл папки сохраняется в
  IndexedDB и переживает перезапуск браузера. Каждый новый сеанс Chrome
  один раз показывает полоску «Разрешить чтение/запись?» — это политика
  самого Chrome, ускорить нельзя.
- Service-worker `background.js` раз в 12 часов проверяет версию через
  `raw.githubusercontent.com/.../manifest.json`, кладёт результат в
  `chrome.storage.local["update"]`. В попапе есть ссылка «Проверить
  обновления» для ручного запуска.
- Если File System Access API недоступен (старый браузер, форк), визард
  предлагает скачать архив вручную и не падает.

### Changed
- В `manifest.json` добавлены permissions `alarms` и host-permissions для
  `api.github.com`, `raw.githubusercontent.com`, `codeload.github.com`
  (нужны для проверки версии и скачивания zipball'а).
- В попапе расширения внизу появилась ссылка «Проверить обновления» с
  индикатором результата.

### Notes
- Источник архива — `https://api.github.com/repos/motlich1998-dotcom/F5---/zipball/main`.
  Версия определяется по `manifest.json` внутри архива, без отдельных
  GitHub Releases.
- Раз в 12 часов идёт один внешний запрос к GitHub. Если для клиентов это
  чувствительно — отключите фоновую проверку, удалив permission `alarms`,
  или просто не открывайте попап (без него запрос не делается; сам алярм
  делает 1 GET за период проверки).

## [1.2.0] — 2026-05-15

### Added
- Hover-«фартук» над лаунчером F5: при наведении на кнопку появляется быстрый
  шорткат `{…}` для шаблонизатора. Клик по самой F5 открывает расшифровщик
  как и раньше.
- Подсветка активного состояния: иконка `{…}` (и в фартуке, и в шапке
  расшифровщика) подсвечивается тёмным, когда шаблонизатор открыт — сразу
  видно, в каком он состоянии.
- Активное (последнее тронутое) окно автоматически выходит на передний план
  поверх соседнего — как у обычных окон в ОС. Реализовано через одинаковый
  базовый `z-index` у обоих окон и динамический класс `is-front` на mousedown.
- Загрузка пользователей (`/api/v4/users`) и групп (`/api/v4/account?with=users_groups`)
  в словарь домена. Во вкладке «Пользователь» шаблонизатора теперь видно ID каждого
  сотрудника и каждой группы, юзеры сгруппированы по своим отделам (как этапы под
  воронками); клик копирует ID. Помечаются админы и деактивированные аккаунты.
- ID пользователей и групп подсвечиваются как ID-подсказки в любом тексте
  (тултип показывает имя, e-mail и группу).
- Счётчики «Польз.» и «Группы» в попапе.

### Changed
- Шаблонизатор теперь полностью независимое окно: больше не закрывается вместе
  с расшифровщиком и не открывается автоматически при его запуске. Координаты
  и высота по умолчанию подбираются корректно даже если расшифровщик закрыт
  (правый нижний угол, фиксированная высота 420 px).
- Поиск в шаблонизаторе теперь учитывает имя группы и e-mail пользователя.

### Notes
- Эндпойнт `/api/v4/users` доступен только администраторам аккаунта; для
  не-админских токенов список пользователей будет пустым (расширение это
  обрабатывает молча, остальной словарь грузится как обычно). Группы
  (`users_groups`) тянутся через `/api/v4/account` и доступны всем.

## [1.1.0] — 2026-05-13

### Added
- В шаблонизаторе во вкладке «Воронки» этапы теперь сгруппированы под своими воронками
  с заголовком воронки и подсказкой «основная» для главной — больше нельзя перепутать одинаково
  названные стадии из разных воронок.
- Отображение версии расширения в шапке плавающей панели и в попапе (включая футер popup).
- В попапе появилась кнопка «Кнопка Аммы: показана/скрыта». Скрытие реализовано
  через CSS-инъекцию по стабильным `aria-label` (без привязки к хешированным CSS-классам
  amoCRM). Применяется и снимается мгновенно — перезагрузка вкладки не требуется.
- Файл `CHANGELOG.md`.

### Changed
- В шаблонизаторе при поиске для этапов в правой колонке теперь показывается их воронка
  («Этап · <Название воронки>»).

### Fixed
- Вложенные переменные внутри модификаторов (например, `:if(=,1,{{lead.sale}},{{lead.name}})`)
  теперь распознаются и интерактивны: на каждую можно навести мышь для подсказки и
  кликнуть, чтобы раскрыть. Реализовано через рекурсивный рендер содержимого аргументов
  модификатора.

## [1.0.0] — 2026-05-08

### Added
- Плавающая панель F5 для расшифровки переменных amoCRM/Kommo (`{{lead.cf(123)}}`,
  `{{contact.sf(...)}}`, `{{user.name}}` и т.п.).
- Шаблонизатор: список переменных аккаунта со вкладками («Сделка», «Контакт», «Компания»,
  «Каталоги», «Воронки», «Системные»), поиском, выбором модификатора и кликом для копирования
  в буфер обмена.
- Подсказки по модификаторам (`:df`, `:if`, `:case`, `:upc`, `:lc`, `:trim` и др.) с описанием
  и примерами.
- Распознавание «голых» ID (полей, воронок, этапов) и тултипы с расшифровкой.
- Перетаскиваемые и масштабируемые панели с сохранением размеров и положения.
- Обновление словаря через API текущего аккаунта (cookie/session) — никаких внешних запросов.
